import 'package:flutter/material.dart';

class adm extends StatelessWidget {
  const adm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text(";admin"),);
  }
}